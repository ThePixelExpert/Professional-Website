# =============================================================================
# Edwards Engineering Frontend - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in the values
# NEVER commit .env to git - it contains secrets!
#
# Note: React environment variables MUST be prefixed with REACT_APP_
#       to be included in the build.
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase Configuration (Frontend)
# -----------------------------------------------------------------------------
# Use the SAME values from contact-backend/.env
#
# For LOCAL development:
#   REACT_APP_SUPABASE_URL=http://localhost:54321
#   REACT_APP_SUPABASE_ANON_KEY=(same as backend SUPABASE_ANON_KEY)
#
# For PRODUCTION:
#   REACT_APP_SUPABASE_URL=https://supabase.edwardstech.dev
#   REACT_APP_SUPABASE_ANON_KEY=(same as backend SUPABASE_ANON_KEY)
# -----------------------------------------------------------------------------

REACT_APP_SUPABASE_URL=
REACT_APP_SUPABASE_ANON_KEY=
