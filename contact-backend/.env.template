# Backend Environment Configuration Template
# Copy this to .env and fill in your actual values

# Server Configuration
PORT=3001

# Admin Credentials - CHANGE THESE!
ADMIN_USER=admin
ADMIN_PASS=password
JWT_SECRET=your_very_long_random_secret_key_here_at_least_32_chars

# Email Configuration (Gmail)
EMAIL_USER=email
EMAIL_APP_PASSWORD=password

# Frontend URL
FRONTEND_URL=https://edwardstech.dev

# Instructions:
# 1. Rename this file to .env (remove .template)
# 2. Replace ADMIN_USER, ADMIN_PASS, and JWT_SECRET with secure values
# 3. Never commit the .env file to git
# 4. For production deployment, these are already configured in Kubernetes secrets