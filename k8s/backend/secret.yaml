apiVersion: v1
kind: Secret
metadata:
  name: email-secret
type: Opaque
data:
  # Replace these with your base64 encoded values:
  # echo -n "your-email@gmail.com" | base64
  # echo -n "your-app-password" | base64
  email-user: <BASE64_ENCODED_EMAIL>
  email-password: <BASE64_ENCODED_APP_PASSWORD>